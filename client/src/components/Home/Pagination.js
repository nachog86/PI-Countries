import React from 'react';

const Pagination = () => {
    return (
        <div>
            <button>Anterior</button>
            <button>Siguiente</button>
        </div>
    )
}

export default Pagination;
